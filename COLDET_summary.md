# COLDET — סיכום

**כלל:** כשנוצר סיכום חדש — מוחקים את הישן מיד.
**פורמט:** מילה באנגלית בתוך משפט עברי — שורה חדשה לפניה ואחריה.

---

## מה זה COLDET

תוסף AutoLISP ל-ZWCAD שמצייר בלוק תיעוד של פרטי עמוד (column detail).
קבצים: `COLDET.lsp` + `COLDET.dcl` בתיקייה `C:\Users\Owner\Desktop\claude\Acad\coldet\`

---

## דרישת תאימות

הקוד חייב לעבוד על **AutoCAD** ועל **ZWCAD** כאחד.

---

## מה עובד — עמוד מלבני

- פקודת COLDET מזהה עמוד מלבני ומציירת עליו גיאומטריה חיצונית ופנימית ✓
- דונאטים פינתיים ממוקמים נכון (אופסט שווה מהקו האופקי והאנכי) ✓
- דונאטים על הצלעות ✓
- קווי ליידר ✓
- אוגן — ממוקם מימין לחתך החיצוני עם רווח 15 יחידות, ממורכז אנכית ✓
- כותרת עם מספר עמוד וקנה מידה ✓
- טעינת הגדרות — תמיד ממוזגת עם ברירות מחדל (לא ייתכן מפתח חסר) ✓

---

## מה עובד — עמוד צורת ר (L-shape)

### שלב 1 — זיהוי

מהנקודות ה-6 של הצורה, הקוד מוצא:

- **פינה חסרה** — הפינה שהיא פינת ה-
`bbox`
הכולל אבל **לא** קיימת ברשימת הנקודות
- **נקודת קעור** (
`concave vertex`
) — הנקודה שה-
`x`
וה-
`y`
שלה נמצאים **שניהם** בתוך ה-
`bbox`
ולא על הקצוות

**פונקציות:**
`cdt:lshape-missing-corner` , `cdt:lshape-concave-vertex`

---

### שלב 2 — פירוק לשני מלבנים

לפי הפינה החסרה, הנקודה הקעורה מחלקת את הצורה ל-2 מלבנים לא-חופפים:

| פינה חסרה | מלבן גדול (r1) | מלבן קטן-רגל (r2) |
|-----------|---------------|------------------|
| BL | (x0,cy)→(x1,y1) | (cx,y0)→(x1,cy) |
| BR | (x0,cy)→(x1,y1) | (x0,y0)→(cx,cy) |
| TR | (x0,y0)→(x1,cy) | (x0,cy)→(cx,y1) |
| TL | (x0,y0)→(x1,cy) | (cx,cy)→(x1,y1) |

**פונקציה:**
`cdt:lshape-bboxes`
— מחזירה `(list גדול קטן)` לפי שטח

---

### שלב 3 — קו חיצוני

העתק הצורה המקורית (לא שני מלבנים) על שכבת
`ext-layer`
, בעזרת:

```lisp
(command "_.COPY" (ssadd ename (ssadd)) "" '(0 0 0) '(0 0 0))
```

---

### שלב 4 — קווים פנימיים ודונאטים

לכל
`bbox`
בנפרד: קו פנימי עם
`offset`
+ דונאטים.

#### כללי דונאטים (זהים למלבן רגיל):

**פינות:**
- 4 דונאטים — אחד בכל פינת המלבן הפנימי
- מרחק שווה מהקו האופקי ומהקו האנכי

**צלעות:**
- מרווח מקסימלי:
`CDT:MAX-SPACING = 18.0`
- אם הצלע ארוכה מ-
`MAX-SPACING`
— מחולקת לקטעים שווים

**מניעת כפילויות — חשוב לצורת ר:**
- רשימת כל הדונאטים שכבר הונחו מועברת בין שני המלבנים
- פינות משותפות: דונאט מונח פעם אחת בלבד
- הצלע המשותפת בין שני המלבנים: **אין עליה דונאטים כלל**

**פונקציות:**
`cdt:pt-near-p` (בדיקת קרבה) ,
`cdt:donut-if-new` (מניח רק אם חדש) ,
`cdt:place-corner-donuts-tracked` ,
`cdt:place-edge-donuts-tracked`

---

### שלב 5 — ליידרים

2 ליידרים — אחד לכל
`bb-int`

---

### שלב 6 — אוגנים

2 אוגנים:
- אוגן 1: מימין לצורה הכוללת + 15 יחידות, ממורכז אנכית
- אוגן 2: מימין לאוגן 1 + 15 יחידות, ממורכז אנכית

**טקסט האוגן** — ייקבע דרך שילוב עם תוסף
`BARS`
(ראה משימה #2)

---

### שלב 7 — כותרת

`top-y`
ו-
`cx`
של הצורה הכוללת

---

## בעיות ידועות — ממתינות לתיקון

### 1. הגדרות לא נשמרות
**תסמין:** שינויים בחלון ההגדרות לא נשמרים בין ריצות.
**סיבה חשודה:** ב-ZWCAD, `getkword` מחזיר `"S"` במקום `"Settings"` — ולכן הבדיקה `(= kw "Settings")` נכשלת.
**תיקון שנכתב:** `(or (= kw "Settings") (= kw "S"))` — טרם אושר בבדיקה.

### 2. טקסט של אוגן לא נוצר
**תסמין:** מספרי הגובה והרוחב על האוגן לא מצוירים.
**סיבה חשודה:** `(command "_.TEXT" "J" "MC" ...)` לא עובד ב-ZWCAD.
**מיקום:** `cdt:draw-stirrup-rect` שורות 401–410, `cdt:draw-text-center` שורה 245.

### 3. טקסט של ליידר (דונאטים) לא נוצר
**תסמין:** הטקסט בנקודת חיבור הליידר לא מצויר.
**סיבה חשודה:** אותה בעיה — `(command "_.TEXT" "J" "MC" ...)`.
**מיקום:** `cdt:leaders-rect` קוראת ל-`cdt:draw-text-center`.

---

## שילוב עם BARS — משימה #2

טקסט האוגן (בצורת ר ובמלבן) יגיע מתוסף
`BARS`
.

`BARS`
= תוסף נפרד לציור תוויות פרטי ברזל.
מיקום: `C:\Users\Owner\Desktop\claude\Acad\bars\`
גיטהאב: `https://github.com/ashirelavi-netizen/bars.git`

השלב הבא: לשלב את
`BARS`
עם
`COLDET`
כך שלחיצה על "עריכת תווית" ב-Settings תפעיל את
`BARS`
ותצייר טקסט על האוגן.

---

## בעיות ZWCAD ידועות

| פונקציה | AutoCAD | ZWCAD | פתרון |
|---------|---------|-------|--------|
| `findfile` | מחזיר נתיב | מחזיר `T` | בדוק `stringp` לפני שימוש |
| `(or "val" "")` | מחזיר `"val"` | מחזיר `T` | השתמש ב-`(if (stringp v) v "")` |
| `start_dialog` | מחזיר מספר | מחזיר `T` | אל תסמוך על ערך החזרה |
| הערות DCL | תומך `//` ו-`/* */` | תומך רק `/* */` | השתמש רק ב-`/* */` |
| `getkword` עם `initget "Settings"` | מחזיר `"Settings"` | אולי מחזיר `"S"` | בדוק את שניהם |
| `command "_.TEXT" "J" "MC"` | עובד | לא עובד | דרוש תיקון — להחליף ב-`entmake` |

---

## גישת ההגדרות

| מצב | פעולה |
|-----|--------|
| אין קובץ | שמירת ברירות מחדל + ריצה רגילה |
| קובץ קיים | טעינה + מיזוג עם ברירות מחדל |
| כל ריצה | `cdt:merge-cfg` מבטיח שכל המפתחות קיימים |

קובץ הגדרות: `coldet_settings.dat` בתיקיית הפרויקט
פורמט: `key|value` שורה לכל שדה

---

## גישת הפעלת הפקודה

```
COLDET
אפשרויות [Settings] <בחר עמוד>:   ← Enter להמשך, S להגדרות
הצבע על קו החוץ של העמוד:         ← בחר עמוד מלבני
```

---

## מבנה COLDET.lsp

```
A.  הגדרות — שמירה/טעינה
B.  כלי הצבעה
C.  מתמטיקה
D.  קריאת XData
E.  זיהוי צורה
F.  כלי מלבן (bbox)
G.  ציור בסיסי (rect, line, donut, text)
H.  דונאטים פינתיים + צלעות (כולל tracked לצורת ר)
I.  קווי ליידר
J.  אוגן (stirrup)
K.  כותרת
L.  חלון הגדרות DCL
M.  C:COLDET — פקודה ראשית
```

---

## עדיין בפיתוח

- עמוד עגול (stub)
- שילוב עם BARS לטקסט האוגן (משימה #2)
